apiVersion: batch/v1
kind: Job
metadata:
  name: ttl-job
  namespace: default
spec:
  ttlSecondsAfterFinished: 100
  template:
    spec:
      containers:
      - name: worker
        image: busybox:1.35
        command: ['sh', '-c', 'echo "Job completed" && sleep 10']
      restartPolicy: Never

