apiVersion: batch/v1
kind: Job
metadata:
  name: deadline-job
  namespace: default
spec:
  activeDeadlineSeconds: 300
  template:
    spec:
      containers:
      - name: worker
        image: busybox:1.35
        command: ['sh', '-c', 'sleep 400']  # Will exceed deadline
      restartPolicy: Never
