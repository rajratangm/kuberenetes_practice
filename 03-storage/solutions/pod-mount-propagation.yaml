apiVersion: v1
kind: Pod
metadata:
  name: pod-mount-propagation
  namespace: default
spec:
  containers:
  - name: writer
    image: busybox:1.35
    command: ['sh', '-c', 'echo "data" > /shared/data.txt && sleep 3600']
    volumeMounts:
    - name: shared-volume
      mountPath: /shared
      mountPropagation: Bidirectional
  - name: reader
    image: busybox:1.35
    command: ['sh', '-c', 'cat /shared/data.txt && sleep 3600']
    volumeMounts:
    - name: shared-volume
      mountPath: /shared
      mountPropagation: Bidirectional
  volumes:
  - name: shared-volume
    emptyDir: {}

